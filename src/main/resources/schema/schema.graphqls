type Query {
    user(username: String): User

    users: [User]
}

type Mutation {
    createUser(username: String): User!
}

type User {
    id: ID
    username: String
}

type Post {
    id: ID
    userId: Int
    content: String
}
