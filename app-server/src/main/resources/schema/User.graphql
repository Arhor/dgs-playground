input CreateUserRequest {
    username: String!
    password: String!
    settings: Settings
}

type User implements Indentifiable {
    id: ID!
    username: String!
    settings: Settings
    posts: [Post!]
}
